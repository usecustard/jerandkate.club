{
  "name": "jerandkate.club",
  "version": "0.0.1",
  "description": "Jeremy & Kate's Rooftop Wedding Extravaganza",
  "private": true,
  "scripts": {
    "dev": "bundle exec jekyll serve",
    "dev:netlify": "netlify dev",
    "build": "bundle exec jekyll build",
    "build:netlify": "netlify build",
    "deploy": "netlify deploy --prod",
    "deploy:preview": "netlify deploy"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
